# Deprecated

This Angular app has been superseded by the new React monorepo.

Use the root project instead:

```bash
cd ..
pnpm install
pnpm dev
```

See the root [README](../README.md) for details.
